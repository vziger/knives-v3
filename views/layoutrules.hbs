<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" name="viewport" />
  <meta name="format-detection" content="telephone=no">
  
  <meta property="og:image" content="/images/preview_all.jpg">
  <meta property="vk:image" content="/images/preview_vk.jpg">
  <meta property="og:title" content="Онлайн-чемпионат по метанию ножей и топоров">
  <meta property="og:description" content="10 июня объявим победителей второго этапа">
  <meta property="og:type" content="website">

  <title>{{title}}</title>

  <link rel="shortcut icon" href="/images/favicon.png" type="image/x-icon">
  
  <link rel="preload" href="/fonts/Montserrat-Black.woff" as="font" type="font/woff" crossorigin>
  <link rel="preload" href="/fonts/Montserrat-Bold.woff" as="font" type="font/woff" crossorigin>
  <link rel="preload" href="/fonts/Montserrat-ExtraBold.woff" as="font" type="font/woff" crossorigin>
  <link rel="preload" href="/fonts/Montserrat-Medium.woff" as="font" type="font/woff" crossorigin>
  <link rel="preload" href="/fonts/Montserrat-Regular.woff" as="font" type="font/woff" crossorigin>
  <link rel="preload" href="/fonts/Montserrat-SemiBold.woff" as="font" type="font/woff" crossorigin>

  {{!-- <link rel="stylesheet" href="/stylesheets/normalize.css"> --}}
  <link rel="stylesheet" href="/stylesheets/fonts.css">
  <link rel="stylesheet" href="/stylesheets/rules.css">
  <link rel="stylesheet" href="/stylesheets/rules_wide.css">

</head>
<script>
  let currentIndex = 1;

  window.onresize = function() {
    const imagesEl = document.querySelector('.carousel__images');
    const viewPortWidth = window.innerWidth;

    if (viewPortWidth < 1201) {
      imagesEl.style = `left: ${74 - 74 * currentIndex}vw;`;
    } else {
      imagesEl.style = `left: ${888 - 888 * currentIndex}px;`;
    }

  };

  function setMainImage(index) {
    const imagesEl = document.querySelector('.carousel__images');
    const viewPortWidth = window.innerWidth;

    currentIndex = index;

    if (viewPortWidth < 1200) {
      imagesEl.style = `left: ${74 - 74 * index}vw;`;
    } else {
      imagesEl.style = `left: ${888 - 888 * index}px;`;
    }

    const oldActiveEl = document.querySelector('.carousel__menu-button_active');
    oldActiveEl.classList.remove('carousel__menu-button_active');

    const newActiveEl = document.querySelector(`.carousel__menu-button:nth-child(${index})`);
    newActiveEl.classList.add('carousel__menu-button_active');
  }
</script>

<body>
    {{{body}}}
</body>
</html>